///////////////////////////////////////////////////////////////////////////////
///         University of Hawaii, College of Engineering
/// @brief  _ee205_lab_08d_animal_farm_1_to_clion - EE 205 - Spr 2022
///
/// @file updateCats.cpp
/// @version 1.0
///
/// @author  Aksel Sloan <@aksel@hawaii.edu>
/// @date   20_Mar_2022
///////////////////////////////////////////////////////////////////////////////
#include <stdio.h>
#include <string.h>
#include <stdbool.h>

#include "catDatabase.h"
#include "updateCats.h"
#include "config.h"
/*


bool updateCatName ( const int currentCat, const char newCatName[] ){
    if (  validateName ( newCatName )  == false){
        return false;
    }
    if (  validateIndex ( currentCat )  == false){
        return false;
    }
    //problem because test is failing
    strcpy ( cats[ currentCat ].name ,newCatName );
    return true;
}

bool fixCat ( const int currentCat ){
    if ( validateIndex ( currentCat ) == false ){
        return false;
    }
    cats[ currentCat ].isFixed = true;
    return true;
}

bool updateWeight ( const int currentCat, const Weight newWeight ){
    if ( validateWeight ( newWeight ) == false ){
        return false ;
    }
    cats[ currentCat ].weight = newWeight;
    return true;
}

bool updateCollarColor1 ( const int currentCat, enum Color newCollarColor1 ){
    cats[ currentCat ].collarColor1 = newCollarColor1;
    return true;
}

bool updateCollarColor2 ( const int currentCat, enum Color newCollarColor2 ){
    cats[ currentCat ].collarColor2 = newCollarColor2;
    return true;
}

bool updateLicense ( const int currentCat, const unsigned long long newLicense ){
    cats[ currentCat ].license = newLicense;
    return true;
}
*/
